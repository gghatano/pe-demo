"""pe-demo project code (additional experiments on top of the official DPSDA Tab-PE).

This package holds our own code (e.g. custom embeddings) kept separate from the
vendored upstream `pe` package. See docs/research/ for design notes.
"""
